import Link from "next/link";
import { articles } from "@/data/articles";
import { Container } from "@/components/ui/Container";

export function ArticlesSection() {
  return <section className="bg-surface py-16 sm:py-24" aria-labelledby="resources-heading"><Container><div className="grid gap-6 border-b border-navy/20 pb-7 lg:grid-cols-[1fr_auto] lg:items-end"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-gold">Field notes</p><h2 id="resources-heading" className="mt-3 text-3xl font-semibold tracking-tight text-navy sm:text-4xl">먼저 겪은 사람의 노트.</h2></div><Link href="/resources" className="inline-flex min-h-11 items-center text-sm font-semibold text-navy">전체 자료 보기 <span className="ml-3" aria-hidden="true">→</span></Link></div><ol>{articles.map((article,index) => <li key={article.slug} className="border-b border-border"><Link href={`/resources/${article.slug}`} className="group grid gap-4 py-7 transition hover:bg-white/60 sm:grid-cols-[4rem_1fr_1.2fr_auto] sm:items-center sm:px-4"><span className="font-mono text-sm text-gold">0{index+1}</span><span><span className="block text-xs font-semibold uppercase tracking-[0.14em] text-muted">{article.category} · {article.readingTime}</span><strong className="mt-2 block text-lg leading-7 text-navy">{article.title}</strong></span><span className="text-sm leading-6 text-muted">{article.description}</span><span className="text-xl text-navy transition group-hover:translate-x-1" aria-hidden="true">→</span></Link></li>)}</ol></Container></section>;
}
